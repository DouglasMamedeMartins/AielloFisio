import styled from 'styled-components';

export const FooterEl = styled.footer`
  background: #081523;
  padding: 40px 5%;
  text-align: center;

  p {
    color: rgba(255, 255, 255, 0.4);
    font-size: 0.85rem;
    line-height: 1.8;
  }

  span {
    color: #d4af37;
  }

  small {
    opacity: 0.6;
  }
`;
